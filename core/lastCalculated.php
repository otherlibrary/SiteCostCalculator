<?phpinclude('config.php');$lang = $_GET['lang'];if ($lang) {include($lang.".php");} else {include("en.php");} if (!empty($_SERVER['HTTP_CLIENT_IP']))   //check ip from share internet { $ip=$_SERVER['HTTP_CLIENT_IP']; } elseif (!empty($_SERVER['HTTP_X_FORWARDED_FOR']))   //to check ip is pass from proxy { $ip=$_SERVER['HTTP_X_FORWARDED_FOR']; } else { $ip=$_SERVER['REMOTE_ADDR']; }		$tbl_name="worth";	$adjacents = 1;	$total_pages = 100;	$targetpage = "lastCalculated.php";	$limit = 14;	$page = $_GET['page'];	if($page) { 		$start = ($page - 1) * $limit;	} else {		$start = 0;	}	$sql = "SELECT DISTINCT domain, worth, dpview, dearn FROM worth ORDER BY id DESC LIMIT $start, $limit";	$result = mysql_query($sql);		/* Setup page vars for display. */	if ($page == 0) $page = 1;	$prev = $page - 1;	$next = $page + 1;	$lastpage = ceil($total_pages/$limit);	$lpm1 = $lastpage - 1;	$pagination = "";	if($lastpage > 1)	{			$pagination .= "<div class=\"pagination\">";		if ($page > 1) 			$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$prev."');\"><</a>";		else			$pagination.= "<span class=\"disabled\"><</span>";			if ($lastpage < 7 + ($adjacents * 2))		{				for ($counter = 1; $counter <= $lastpage; $counter++)			{				if ($counter == $page)					$pagination.= "<span class=\"current\">$counter</span>";				else					$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$counter."');\">$counter</a>";								}		}		elseif($lastpage > 5 + ($adjacents * 2))	//enough pages to hide some		{			if($page < 1 + ($adjacents * 2))					{				for ($counter = 1; $counter < 4 + ($adjacents * 2); $counter++)				{					if ($counter == $page)						$pagination.= "<span class=\"current\">$counter</span>";					else						$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$counter."');\">$counter</a>";									}				$pagination.= "...";				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$lpm1."');\">$lpm1</a>";				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$lastpage."');\">$lastpage</a>";					}			elseif($lastpage - ($adjacents * 2) > $page && $page > ($adjacents * 2))			{				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('1');\">1</a>";				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('2');\">2</a>";				$pagination.= "...";				for ($counter = $page - $adjacents; $counter <= $page + $adjacents; $counter++)				{					if ($counter == $page)						$pagination.= "<span class=\"current\">$counter</span>";					else						$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$counter."');\">$counter</a>";									}				$pagination.= "...";				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$lpm1."');\">$lpm1</a>";				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$lastpage."');\">$lastpage</a>";					}			else			{				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('1');\">1</a>";				$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('2');\">2</a>";				$pagination.= "...";				for ($counter = $lastpage - (2 + ($adjacents * 2)); $counter <= $lastpage; $counter++)				{					if ($counter == $page)						$pagination.= "<span class=\"current\">$counter</span>";					else						$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$counter."');\">$counter</a>";									}			}		}		if ($page < $counter - 1) 			$pagination.= "<a style=\"cursor:pointer;\" onclick=\"lastCalculated('".$next."');\">></a>";		else			$pagination.= "<span class=\"disabled\">></span>";		$pagination.= "</div>\n";	}	$t1 = $ccc1;	$res = "<center><span id='lastTitle'><b>".$t1."</b></span><table style=\"width:300px;\">";while ($row = mysql_fetch_assoc($result)) { 	$domain=$row[domain];		 $worth=$row[worth];		 $worthInt = intval($worth);		 if ($worthInt < 100) {			$temp_ = $worthInt + 100;			$worthStr = "$".$temp_;		 } else {			$worthStr = "$".number_format($worthInt, 0, '.', ',');		 }		 $dpview=$row[dpview];		 $dpviewInt = intval($dpview);if ($dpviewInt > 10000) {		 $dpviewInt = $dpviewInt / 10;		 $dpviewInt = round($dpviewInt);}		 if ($dpviewInt < 20) {		 	$dp = '< 20';		 } else {		 	$dp = number_format($dpviewInt, 0, '.', ',');		 }		 $dearn=$row[dearn];		 $dearnInt = intval($dearn);		 $dearnInt = $dearnInt * 11;		 		 if ($dearnInt == 0) {		 	$dearnInt = 1;		 }	$res = $res."<translate><td align='center'><a style=\"text-decoration:none;\" onclick=loadS('".$domain."'); style=\"text-decoration:none; cursor:pointer;\" href=\"index.php?d=".$domain."\">		<div style=\"background-color:#EDF8FC; border:1px dashed;margin: 2px 2px 2px 2px; border-color: #E6E6E6 #E6E6E6 #E6E6E6 #E6E6E6;cursor:pointer;\" onmouseover=\"this.style.border = '1px dashed black';\" onmouseout=\"this.style.border = '1px dashed #E6E6E6';\">		<center>		   <b><span style='font-family: georgia, serif; font-size:12pt; color:black;'>".$domain."</span></b><br><font style='color:green;'>		   ".$worthStr."</font><br> 		</center>		</div>		   </a></td></translate>";				};$res = $res."</table></center>";mysql_close($con);echo $res."<center>".$pagination."</center>";?>